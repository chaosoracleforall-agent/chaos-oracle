{
  "name": "chaos-oracle-root",
  "private": true,
  "version": "1.0.54",
  "scripts": {
    "build": "cd packages/frames && npm install && npm run build",
    "install": "cd packages/frames && npm install"
  }
}
